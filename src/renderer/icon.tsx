import { Renderer } from "@freelensapp/extensions";

export function RabbitmqIcon(props: Renderer.Component.IconProps) {
  return <Renderer.Component.Icon {...props} material="mail" tooltip="RabbitMQ" />;
}
